# AI Ink Scholar X · UI 设计方案

> 由 UI Designer 构建 · 明亮 / 简洁 / 清新 风格 · 2026-07-25

本方案为「AI Ink Scholar X」（AI 辅助小说创作工具）设计一套全新界面，从 **设计系统** 出发，保证视觉一致性、操作符合常见习惯、跨屏幕良好适配。可交互原型见 `prototype/index.html`。

---

## 一、设计原则

| 原则 | 落地方式 |
|------|----------|
| **简洁清新** | 浅灰蓝背景 `#F6F8FC` + 纯白卡片，大面积留白，8pt 间距节奏 |
| **明亮活力** | 主色清爽蓝 `#4F7CFF` + 辅助青绿 `#14B8A6`，渐变仅用于 logo/封面点缀 |
| **符合习惯** | 左侧固定导航 + 顶栏全局操作，信息架构贴近 Notion / 飞书 等常见工作台 |
| **一致规范** | 全部组件基于同一套 Design Tokens，颜色/圆角/阴影/动效统一引用 |
| **无障碍** | WCAG AA 对比度、44px 触控目标、键盘可见焦点、尊重 reduced-motion |

---

## 二、配色方案（Color System）

### 主色与辅助色
| 用途 | Token | 值 | 说明 |
|------|-------|-----|------|
| 主色-默认 | `--c-primary-500` | `#4F7CFF` | 主按钮、激活态、链接 |
| 主色-悬停 | `--c-primary-600` | `#3D6BF0` | hover 加深 |
| 主色-浅底 | `--c-primary-50` | `#EEF3FF` | 激活导航背景、聚焦光晕 |
| 辅助色 | `--c-accent-500` | `#14B8A6` | 徽章、进度点缀、次要强调 |

### 语义色（Semantic）
| 类型 | 值 | 用途 |
|------|-----|------|
| 成功 `--c-success` | `#22C55E` | 已完成、正向趋势 |
| 警告 `--c-warning` | `#F59E0B` | 待审核、提示 |
| 错误 `--c-error` | `#EF4444` | 校验失败、负向趋势 |
| 信息 `--c-info` | `#4F7CFF` | 通知、说明 |

### 中性色（Neutral）
| 层级 | Token | 值 |
|------|-------|-----|
| 页面背景 | `--c-bg` | `#F6F8FC` |
| 卡片表面 | `--c-surface` | `#FFFFFF` |
| 次级表面 | `--c-surface-2` | `#FBFCFE` |
| 边框 | `--c-border` | `#E8EDF5` |
| 主文本 | `--c-text` | `#1E293B` |
| 次文本 | `--c-text-2` | `#475569` |
| 弱文本 | `--c-text-3` | `#94A3B8` |

### 暗色主题
通过 `[data-theme="dark"]` 覆盖背景/表面/文本/边框，主色与辅助色保留识别度（如 `--c-primary-300` 提亮用于图标）。原型已支持一键切换并记忆偏好。

> 对比度：主文本 `#1E293B` on `#FFFFFF` ≈ 13:1；主按钮白字 on `#4F7CFF` ≈ 4.6:1，均满足 WCAG AA（正文 4.5:1 / 大字 3:1）。

---

## 三、排版与间距（Typography & Spacing）

- **字体**：`Inter` + 中文回退 `PingFang SC / Microsoft YaHei`（系统字体优先，零加载成本）。
- **字号阶梯**：12 / 14 / 16 / 18 / 22 / 28 / 36（px），行高 1.5–1.55。
- **字重**：400 常规 / 500 中 / 600 半粗 / 700–800 标题粗。
- **间距系统（8pt 基础）**：4 / 8 / 12 / 16 / 20 / 24 / 32 / 40 / 48，统一驱动内边距、卡片间隙与栅格。
- **圆角**：按钮/输入 8–12px，卡片 16px，胶囊 999px。
- **阴影**：`--sh-sm/md/lg` 三级，hover 升一级营造“浮起”反馈。

---

## 四、页面布局（Layout）

采用 **固定侧栏 + 顶栏 + 内容区** 的经典工作台三区结构：

```
┌────────────┬───────────────────────────────────────────┐
│            │  顶栏: 面包屑 · 搜索 · 主题 · 通知 · 头像     │
│  侧边导航   ├───────────────────────────────────────────┤
│  (264px)   │  内容区 (max 1280px 居中)                   │
│            │  · 欢迎头部 + 新建作品                       │
│  logo      │  · 4 张统计卡（响应式 4→2→1）               │
│  导航项     │  · 双栏：继续创作 / 创作动态                │
│  升级卡片   │  · 我的作品网格（auto-fill 卡片）           │
│            │  · 组件预览（设计规范展示）                  │
└────────────┴───────────────────────────────────────────┘
```

- **侧边导航**：分组（工作区 / 智能 / 系统），激活项左侧色条 + 浅底 + 加粗，未读数量用胶囊徽章。
- **顶栏**：毛玻璃半透明 + 模糊，搜索框聚焦时平滑展开，全局操作右对齐。
- **内容层次**：页面标题 → 关键指标 → 进行中重点 → 列表/网格，视线自上而下递减。

---

## 五、组件样式（Components）

### 按钮 `.btn`
| 变体 | 样式 | 用途 |
|------|------|------|
| Primary | 蓝底白字 + 阴影 | 主要行动（新建/保存） |
| Secondary | 白底描边 | 次级操作 |
| Ghost | 透明蓝字 | 低强调（取消/查看） |
- 统一 `padding 12×20`、圆角 12px、hover 上移 1px 并升级阴影；`:focus-visible` 显示 2px 蓝色描边。

### 输入框 `.input`
白底描边，聚焦时边框变蓝 + 3px 浅蓝光晕（`box-shadow 0 0 0 3px --c-primary-50`），符合常见表单习惯。

### 卡片 `.card` / `.stat`
白底、1px 边框、`--sh-sm` 阴影，hover 上浮 3–4px。统计卡含图标块（按语义着色）+ 数值（28px 粗体）+ 趋势标签。

### 徽章 `.badge`
胶囊状，蓝/青/绿/琥珀四色对应不同状态；作品封面左上角叠加分类标签。

### 开关 `.switch`
44×26px 触控友好，选中态主色填充、滑块右移，支持键盘聚焦。

### 进度条 `.progress`
圆角轨道 + 蓝→青渐变填充，右侧标注百分比，用于章节完成度。

---

## 六、交互细节（Interaction）

1. **导航切换**：点击导航项即时高亮，面包屑同步更新；移动端自动收起抽屉。
2. **主题切换**：顶栏图标一键 light/dark，状态写入 `localStorage`，刷新保留。
3. **分段筛选**：「全部 / 进行中 / 已完结 / 草稿」胶囊切换，选中态白底浮起。
4. **Toast 反馈**：轻量操作（新建/打开/生成建议）底部居中弹出，2.2s 自动消失。
5. **悬停反馈**：卡片/按钮统一上浮 + 阴影升级，过渡 140–260ms 缓动。
6. **焦点管理**：所有可交互元素 `:focus-visible` 显示清晰描边，支持纯键盘操作。

---

## 七、响应式适配（Responsive）

| 断点 | 布局变化 |
|------|----------|
| ≥1024px（桌面） | 侧栏固定 264px，统计卡 4 列，双栏并排 |
| ≤1024px（平板） | 统计卡 2 列，双栏堆叠为单列 |
| ≤768px（移动） | 侧栏转为抽屉（汉堡按钮 + 遮罩），顶栏精简，内容 padding 收窄 |
| ≤480px（小屏） | 统计卡 1 列，搜索框隐藏，续写卡纵向排列 |

- 作品网格用 `grid-template-columns: repeat(auto-fill, minmax(240px,1fr))`，随宽度自动增减列数。
- 全站 `prefers-reduced-motion` 下关闭过渡动画，照顾前庭敏感用户。

---

## 八、无障碍（Accessibility）

- 颜色对比度全部达到 WCAG AA。
- 触控目标 ≥ 44px（开关、图标按钮均达标）。
- 语义化结构 + `aria-label` 标注图标按钮。
- 键盘可完整操作，焦点顺序合理且可见。
- 文本可随浏览器缩放至 200% 不破版。

---

## 九、交付物与落地建议

- **原型**：`prototype/index.html`（单文件，浏览器直接打开即可预览交互）。
- **设计变量**：直接复用文中的 CSS `:root` Tokens，迁移到 React/Tauri 时映射为 CSS 变量或 Tailwind 主题。
- **下一步**：将组件拆为 React 组件（Button / Card / Stat / NovelCard / Switch），接入 `useNovelStore` 真实数据；暗色主题与现有设置持久化打通。

---
**UI Designer** · 设计系统已就绪，可直接交付前端实现。
